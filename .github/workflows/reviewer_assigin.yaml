name: Auto Assgin Reviewer

on:
  pull_request:
    types: [opened,reopened]
  pull_request_review:
    types: [submitted]

jobs:
  reviewer-assign:
    runs-on: ubuntu-latest
    steps:
      - name: reviewer-assign
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo ${{ toJson(github.event) }}
          echo 'PR Num: ${{ github.event.head.number }}'
          gh pr edit ${{ github.event.head.number }} --add-reviewer mominosin
  reviewer-re-assign:
    if: github.event.review.state == 'approved'
    runs-on: ubuntu-latest
    steps:
      - name: reviewer-assign
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo ${{ toJson(github.event) }}
          echo 'PR Num: ${{ github.event.head.number }}'
          gh pr edit ${{ github.event.head.number }} --add-reviewer mominosin

